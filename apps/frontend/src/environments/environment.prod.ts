export const environment = {
  production: true,
  // Fallback production endpoint. Set this to your deployed backend URL at build time
  // if you are not supplying /assets/config/app-config.json.
  apiBaseUrl: 'https://your-backend-url.example.com',
};
